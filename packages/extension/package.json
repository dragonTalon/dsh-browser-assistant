{
  "name": "bridge-browser",
  "version": "0.2.0",
  "description": "Chrome MV3 extension: side panel + text-only read/operate of a controlled tab through the dsh browser bridge",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "dompurify": "^3.4.11",
    "esbuild": "^0.24.0",
    "marked": "^16.4.2",
    "typescript": "^6.0.3"
  }
}
